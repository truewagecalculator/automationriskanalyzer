{
  "name": "is-job-being-automated",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "gen:jobs": "node ./scripts/generate-jobs-index.mjs",
    "dev": "wrangler pages dev ./public --compatibility-date=2025-12-25",
    "deploy": "wrangler pages deploy ./public --project-name=is-job-being-automated"
  },
  "devDependencies": {
    "wrangler": "^3.80.0"
  }
}
